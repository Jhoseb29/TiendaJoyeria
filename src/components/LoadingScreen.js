import '../styles/loader.css'

const LoadingScreen = () => {
    return (
        <div className='loading'>
            <span className='loader'></span>
        </div>
    );
};

export default LoadingScreen;